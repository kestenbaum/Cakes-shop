import React, {FC} from 'react';

import styles from "@/styles/Profile.module.css"
const Page:FC = () => {
    return (
        <section
            className={styles.wrapper}
        >
            Profile
        </section>
    );
};

export default Page;