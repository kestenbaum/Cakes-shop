import React, {FC} from 'react';

import styles from "@/app/reviews/reviews.module.css"

const Page:FC = () => {
    return (
        <section
            className={styles.wrapper}
        >
            reviews
        </section>
    );
};

export default Page;